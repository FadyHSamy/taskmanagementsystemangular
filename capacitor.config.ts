import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.taskmanagementsystemangular',
  appName: 'taskmanagementsystemangular',
  webDir: 'www'
};

export default config;
